<!DOCTYPE html>
<html lang="en">
  <%- include('head.ejs', {}) %>
  <body>
    <div class="container">
      <%- include('nav.ejs', {username: (username != 0) ? username : 0}) %>
    <h2>SurvAPI - Dashboard</h2>

    <% if (username == 0) { %>
    <div class="row">
      <div class="col">
        <%- include('registerForm.ejs', {}) %>
      </div>
      <div class="col">
        <%- include('loginForm.ejs', {}) %>
      </div>
    </div>
    <% } else {%>
      <div>
        Logged in.
      </div>
    <% } %>
    <%- include('footer.ejs', {}) %>
    </div>

    <%- include('micromodal.ejs', {}) %>
  </body>
</html>
